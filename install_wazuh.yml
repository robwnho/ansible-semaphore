- name: Teste de sudo no playbook
  hosts: local
  become: yes
  gather_facts: no
  become_method: sudo
  become_user: root

  tasks:
    - name: Executar whoami com sudo
      become: yes
      command: whoami
      register: resultado_sudo

    - name: Exibir resultado do sudo
      debug:
        var: resultado_sudo
